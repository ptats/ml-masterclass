import setuptools

setuptools.setup(
    name="utils",
    version="0.0.1",
    description="Utility functions for azure & fast ai",
    packages=setuptools.find_packages(),
    python_requires='>=3.6',
)